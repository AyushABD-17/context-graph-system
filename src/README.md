# Source Code

The source code is organized into two main directories at the root level:

- [`/backend`](../backend/) — FastAPI Python server (graph engine, LLM query, SQLite, semantic search)
- [`/frontend`](../frontend/) — Vite + React app (graph visualization, chat UI, SSE streaming)

See the root [README.md](../README.md) for full setup and deployment instructions.
